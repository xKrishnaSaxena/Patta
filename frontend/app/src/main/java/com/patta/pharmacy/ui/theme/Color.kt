package com.patta.pharmacy.ui.theme

import androidx.compose.ui.graphics.Color

// --- Patta brand palette (from design/DESIGN.md) ---
val PattaTeal = Color(0xFF00685B)          // primary — branding, buttons, voice mic
val PattaTealDark = Color(0xFF004D43)
val PattaTealContainer = Color(0xFFB6E9E0)

val MoneyIn = Color(0xFF16A34A)            // green — money-in, healthy stock
val MoneyInContainer = Color(0xFFDCFCE7)
val ExpiryAmber = Color(0xFFF59E0B)        // amber — near-expiry, low-stock warnings
val ExpiryAmberContainer = Color(0xFFFEF3C7)
val MoneyOut = Color(0xFFDC2626)           // red — money-out, expired, overdue
val MoneyOutContainer = Color(0xFFFEE2E2)

val Surface = Color(0xFFF8F9FF)            // soft grey-blue canvas
val SurfaceCard = Color(0xFFFFFFFF)        // white cards
val SurfaceContainer = Color(0xFFEFF4FF)
val OnSurface = Color(0xFF0B1C30)          // near-black text
val OnSurfaceVariant = Color(0xFF5A6472)   // grey secondary text
val OutlineSoft = Color(0xFFE2E8F0)
